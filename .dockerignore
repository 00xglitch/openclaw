# VCS
.git
.gitignore

# Dependencies (reinstalled in builder)
**/node_modules
.pnpm-store

# Build artifacts (rebuilt in builder)
dist
**/dist
.turbo
coverage

# Platform-specific builds
apps/linux/out
apps/*/out

# Large assets (not needed in gateway image)
*.mp4
*.wav
*.mp3
*.ogg
*.flac
*.avi
*.mov

# Logs and temp
*.log
*.tmp
.env
.env.*

# Docker files (avoid recursive context)
docker/
Dockerfile*
docker-compose*.yml

# IDE / OS
.vscode
.idea
*.swp
*.swo
.DS_Store
Thumbs.db
